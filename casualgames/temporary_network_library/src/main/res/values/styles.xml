<resources>


    <style name="dialogs" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:background">#00000000</item>
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
    </style>

    <style name="smsProgressBarStyleLarge">
        <item name="android:indeterminateDrawable">@drawable/loading</item>
        <item name="android:minWidth">64.0dip</item>
        <item name="android:maxWidth">64.0dip</item>
        <item name="android:minHeight">64.0dip</item>
        <item name="android:maxHeight">64.0dip</item>
    </style>

</resources>
