<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:background="@color/game_user_vip_text"
tools:context="com.weile.casualgames.find.FindFragment">
    <include
        android:id="@+id/home_title"
        layout="@layout/top_navigation"></include>
<FrameLayout
    android:id="@+id/my_message_list_fl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_below="@id/home_title"
    android:background="@color/game_user_vip_text"
    android:visibility="visible">

    <com.weile.casualgames.view.widget.pulltorefresh.PullToRefreshListView
        android:id="@+id/pull_refresh_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/home_title"
        android:cacheColorHint="#00000000"
        android:divider="@null"
        android:fadingEdge="none"
        android:fastScrollEnabled="false"
        android:footerDividersEnabled="false"
        android:headerDividersEnabled="false"
        android:listSelector="@drawable/listview_selector"
        android:scrollbars="none"
        android:smoothScrollbar="true"
        android:descendantFocusability="blocksDescendants" />
</FrameLayout>
<FrameLayout
    android:id="@+id/loadingView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerInParent="true"
    android:background="@color/transparent"
    android:visibility="gone">
    <LinearLayout
        android:id="@+id/dialog_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/transparent"
        android:gravity="center"
        android:minHeight="100dp"
        android:minWidth="180dp"
        android:orientation="vertical"
        android:padding="10dp">
        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminateDrawable="@drawable/loading_progress"
            android:indeterminateDuration="650" />

        <TextView
            android:id="@+id/tv_loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textColor="@color/darkgrey"
            android:textSize="14sp"/>
    </LinearLayout>
</FrameLayout>
<FrameLayout
    android:id="@+id/empty_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerInParent="true"
    android:visibility="gone">
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <ImageView
            android:id="@+id/my_mailbox_empty_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/home_title"
            android:layout_centerHorizontal="true"
            android:src="@mipmap/ic_launcher"/>

        <TextView
            android:id="@+id/my_mailbox_empty_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/my_mailbox_empty_img"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="15dp"
            android:text="大家只看，不说话"
            android:textColor="@color/chat_message_time"
            android:textSize="@dimen/text_size_18" />
    </LinearLayout>
</FrameLayout>

</RelativeLayout>